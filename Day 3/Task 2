#include <stdio.h>

int main() {
    int start, end;
    int evenCount = 0, oddCount = 0;
    int evenSum = 0, oddSum = 0;

    printf("Shkruaj vleren fillestare: ");
    scanf("%d", &start);

    printf("Shkruaj vleren perfundimtare: ");
    scanf("%d", &end);

    // kontrolli i intervalit
    if (start > end) {
        printf("Interval i pavlefshem!\n");
        return 0;
    }

    // for loop
    for (int i = start; i <= end; i++) {
        if (i % 2 == 0) {
            evenCount++;
            evenSum += i;
        } else {
            oddCount++;
            oddSum += i;
        }
    }

    int totalNumbers = evenCount + oddCount;

    printf("\n--- REZULTATET ---\n");
    printf("Intervali: %d - %d\n", start, end);
    printf("Totali i numrave: %d\n", totalNumbers);

    printf("\nNumrat çift: %d\n", evenCount);
    printf("Shuma e numrave çift: %d\n", evenSum);

    printf("\nNumrat tek: %d\n", oddCount);
    printf("Shuma e numrave tek: %d\n", oddSum);

    // kush ka me shume
    if (evenCount > oddCount) {
        printf("\nKa me shume numra çift.\n");
    } else if (oddCount > evenCount) {
        printf("\nKa me shume numra tek.\n");
    } else {
        printf("\nNumrat çift dhe tek jane te barabarte.\n");
    }

    return 0;
}




https://bgtschool-my.sharepoint.com/:w:/g/personal/jp265_bgt_school/IQAdqhJZoartQZrHS5uV7vh4AQkGWDRa9OkDIVVctvsVFd0?e=Jkb8ZL

